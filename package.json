{
  "name": "charging-station-app",
  "version": "1.0.0",
  "description": "Ứng dụng kết nối trạm sạc pin xe máy",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "vercel-build": "prisma generate && cd client && npm install && npm run build",
    "postinstall": "prisma generate",
    "deploy": "npm run build && netlify deploy --prod --dir=client/build",
    "push-deploy": "git add . && git commit -m \"🚀 Auto deploy\" && git push origin main"
  },
  "dependencies": {
    "express": "^4.18.2",
    "@prisma/client": "^5.6.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "prisma": "^5.6.0"
  }
}